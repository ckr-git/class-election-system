# 班级干部评选系统 - 启动指南

## 一、环境准备

### 必需软件
1. **JDK 8+**
   - 下载地址：https://www.oracle.com/java/technologies/downloads/
   - 配置环境变量 JAVA_HOME

2. **Maven 3.6+**
   - 下载地址：https://maven.apache.org/download.cgi
   - 配置环境变量 MAVEN_HOME

3. **MySQL 8.0+**
   - 下载地址：https://dev.mysql.com/downloads/mysql/
   - 默认端口：3306

4. **Redis（可选）**
   - 下载地址：https://redis.io/download
   - Windows版本：https://github.com/tporadowski/redis/releases
   - 默认端口：6379

5. **Node.js 14+**（前端开发）
   - 下载地址：https://nodejs.org/

## 二、数据库配置

### 1. 创建数据库
```sql
CREATE DATABASE election_system DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

### 2. 导入数据库结构
在项目根目录下找到 `database.sql` 文件，执行导入：

**方式1：命令行导入**
```bash
mysql -u root -p election_system < database.sql
```

**方式2：MySQL客户端导入**
- 打开 Navicat/MySQL Workbench 等工具
- 连接到 MySQL 服务器
- 选择 election_system 数据库
- 执行 database.sql 文件

### 3. 初始化数据（可选）

**创建管理员账号：**
```sql
USE election_system;

-- 密码: admin123 (已使用BCrypt加密)
INSERT INTO `user` (username, password, nickname, role, status) 
VALUES ('admin', '$2a$10$N.zmdr9k7uOCQb376NoUnuTJ8iAt6Z5EHsM8lZztnXGKM4jc1ZYJK', '系统管理员', 'ADMIN', 1);
```

**创建测试班级：**
```sql
INSERT INTO `class` (name, grade, major, teacher, student_count) VALUES 
('计算机1班', '2023级', '计算机科学与技术', '张老师', 45),
('计算机2班', '2023级', '计算机科学与技术', '李老师', 48),
('软件工程1班', '2023级', '软件工程', '王老师', 50);
```

**创建职位：**
```sql
INSERT INTO `position` (name, description, responsibilities, max_count, sort_order) VALUES 
('班长', '班级负责人', '负责班级全面工作，协调各项事务', 1, 1),
('副班长', '协助班长工作', '协助班长处理日常班级事务', 2, 2),
('学习委员', '学习事务负责人', '负责班级学习相关事务，收发作业', 1, 3),
('生活委员', '生活事务负责人', '负责班级生活相关事务', 1, 4),
('文艺委员', '文艺活动负责人', '组织策划班级文艺活动', 1, 5),
('体育委员', '体育活动负责人', '组织班级体育活动', 1, 6);
```

## 三、后端启动

### 1. 修改配置文件

打开 `src/main/resources/application.yml`，修改数据库配置：

```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/election_system?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: 你的数据库密码
```

如果不使用Redis，可以注释掉Redis相关配置。

### 2. 编译项目

在项目根目录下执行：
```bash
mvn clean package -DskipTests
```

### 3. 启动后端服务

**方式1：使用Maven命令**
```bash
mvn spring-boot:run
```

**方式2：使用IDE（推荐）**
- 打开 IntelliJ IDEA 或 Eclipse
- 导入项目为 Maven 项目
- 找到主启动类 `ElectionSystemApplication.java`
- 右键运行 `Run 'ElectionSystemApplication'`

**方式3：运行JAR包**
```bash
java -jar target/election-system-1.0.0.jar
```

### 4. 验证启动

启动成功后，访问：http://localhost:8080

看到以下日志说明启动成功：
```
====================================
班级干部评选系统启动成功！
访问地址：http://localhost:8080
接口文档：http://localhost:8080/doc.html (如果集成了Swagger)
====================================
```

## 四、前端启动（暂未实现）

前端将在后续开发中完成，使用Vue2 + Element UI。

```bash
cd frontend
npm install
npm run serve
```

访问：http://localhost:8081

## 五、API接口测试

### 使用Postman测试

#### 1. 用户注册
```
POST http://localhost:8080/api/auth/register
Content-Type: application/json

{
  "username": "20231001",
  "password": "123456",
  "nickname": "张三",
  "classId": 1
}
```

#### 2. 用户登录
```
POST http://localhost:8080/api/auth/login
Content-Type: application/json

{
  "username": "20231001",
  "password": "123456"
}
```

响应示例：
```json
{
  "code": 200,
  "message": "登录成功",
  "data": {
    "token": "eyJhbGciOiJIUzUxMiJ9...",
    "userInfo": {
      "id": 1,
      "username": "20231001",
      "nickname": "张三",
      "role": "STUDENT"
    }
  }
}
```

#### 3. 获取选举列表
```
GET http://localhost:8080/api/election/list?status=2&current=1&size=10
Authorization: Bearer {你的token}
```

#### 4. 获取候选人列表
```
GET http://localhost:8080/api/candidate/list?electionId=1&current=1&size=10
Authorization: Bearer {你的token}
```

#### 5. 提交投票
```
POST http://localhost:8080/api/vote/submit
Authorization: Bearer {你的token}
Content-Type: application/json

{
  "electionId": 1,
  "candidateId": 1
}
```

## 六、常见问题

### 1. 数据库连接失败
- 检查MySQL服务是否启动
- 检查数据库用户名密码是否正确
- 检查数据库名称是否正确

### 2. 端口被占用
修改 `application.yml` 中的端口：
```yaml
server:
  port: 8081  # 改为其他端口
```

### 3. Maven依赖下载失败
配置国内镜像源，修改 `settings.xml`：
```xml
<mirror>
  <id>aliyun</id>
  <mirrorOf>central</mirrorOf>
  <url>https://maven.aliyun.com/repository/public</url>
</mirror>
```

### 4. JWT Token过期
默认过期时间为24小时，可在 `application.yml` 中修改：
```yaml
jwt:
  expiration: 86400  # 单位：秒
```

### 5. 跨域问题
已配置CORS，如遇问题请检查 `CorsConfig.java`

## 七、默认测试账号

### 管理员账号
- 用户名：admin
- 密码：admin123
- 角色：ADMIN

### 学生账号（需自行注册）
- 按照学号格式注册
- 默认角色：STUDENT

## 八、项目目录结构

```
班级干部评选系统/
├── src/main/java/com/election/system/
│   ├── ElectionSystemApplication.java    # 主启动类
│   ├── common/                            # 通用类
│   │   ├── Result.java                   # 统一响应
│   │   └── JwtUtil.java                  # JWT工具
│   ├── config/                           # 配置类
│   │   ├── SecurityConfig.java           # Security配置
│   │   ├── CorsConfig.java               # 跨域配置
│   │   └── MyMetaObjectHandler.java      # 字段自动填充
│   ├── security/                         # 安全相关
│   ├── controller/                       # 控制器
│   ├── entity/                           # 实体类
│   ├── mapper/                           # Mapper接口
│   ├── service/                          # 服务层
│   └── scheduled/                        # 定时任务
├── src/main/resources/
│   ├── application.yml                   # 配置文件
│   └── mapper/                           # MyBatis XML
├── database.sql                          # 数据库脚本
├── pom.xml                              # Maven配置
└── README.md                            # 项目说明
```

## 九、下一步计划

1. ✅ 完成后端核心功能
2. ⏳ 开发Vue2前端界面
3. ⏳ 集成ECharts数据可视化
4. ⏳ 完善权限管理
5. ⏳ 添加文件上传功能
6. ⏳ 部署到服务器

## 十、技术支持

如有问题，请查看项目文档或提交Issue。
